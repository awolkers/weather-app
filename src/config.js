const API_URI = 'https://api.openweathermap.org/data/2.5/'
const APPID = 'aa885728bdd6be93215709d5f3269d23'

const config = {
    api: {
        search: `${API_URI}find?${APPID}&type=like`
    }
}

export default config
